/** Gallery card: 2 widths, WebP only (via Image) — keeps build CPU reasonable */
export const GALLERY_WIDTHS = [800, 1200] as const

/** Photo detail page */
export const DETAIL_WIDTHS = [1200, 1920] as const

/** OGP / Twitter card */
export const OGP_WIDTH = 1200
